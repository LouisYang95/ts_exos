import {filterEvenNumbers} from "./exo5";

describe('Filter the even number', () => {
    it('should return every even number of the table', async () => {
        let numbers = [-200, -163, -26, -4, 0, 7, 76]
        const evenTable = filterEvenNumbers(numbers)
    })
})